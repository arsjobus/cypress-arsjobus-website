# Cypress ARSJobUS Website

This repository is used to organize and run automated UI test cases against the ARSJobUs.GitHub.io portfolio website. 

## Quick Start

1. Clone this repo.
2. Run command `npm i` to install cypress
3. Start Cypress with command:
`npx cypress open`
